---
import Layout from '../../layouts/Layout.astro';

const title = "Credit Card Interest Calculator";

let balance = "";
let interestRate = "";
let months = "";
let totalInterest = null;

// ✅ Server-side POST guard
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  balance = form.get('balance') || "";
  interestRate = form.get('interestRate') || "";
  months = form.get('months') || "";

  const principal = parseFloat(balance);
  const annualRate = parseFloat(interestRate);
  const term = parseInt(months);

  if (!isNaN(principal) && !isNaN(annualRate) && !isNaN(term) && principal > 0 && term > 0) {
    const monthlyRate = annualRate / 100 / 12;
    const total = principal * monthlyRate * term;
    totalInterest = total;
  }
}
---

<Layout>
  <!-- 💳 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">💳 {title}</h1>
    <p class="text-base">Estimate the total interest you'll pay on a credit card balance over time.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Paying only the minimum can rack up thousands in interest. Use this tool to estimate the real cost of carrying a balance.</p>
  </div>

  <!-- 🔢 Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="balance" class="block font-medium text-gray-800">Current Balance ($):</label>
        <input
          type="number"
          step="any"
          name="balance"
          id="balance"
          class="w-full p-2 border rounded"
          value={balance}
          required
        />
      </div>

      <div>
        <label for="interestRate" class="block font-medium text-gray-800">Annual Interest Rate (%):</label>
        <input
          type="number"
          step="any"
          name="interestRate"
          id="interestRate"
          class="w-full p-2 border rounded"
          value={interestRate}
          required
        />
      </div>

      <div>
        <label for="months" class="block font-medium text-gray-800">Time Period (Months):</label>
        <input
          type="number"
          step="1"
          name="months"
          id="months"
          class="w-full p-2 border rounded"
          value={months}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Interest
      </button>
    </form>

    {totalInterest !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Estimated Interest: ${totalInterest.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Credit card interest can accumulate quickly. Understanding how much interest you’ll pay helps you take control of your debt and avoid long-term financial stress.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Try to pay more than the minimum each month to reduce interest.</li>
      <li>Look into 0% introductory APR cards for balance transfers if you qualify.</li>
      <li>Use windfalls like bonuses or tax refunds to pay down debt faster.</li>
    </ul>
  </section>
</Layout>








