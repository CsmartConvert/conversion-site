---
import Layout from '../../layouts/Layout.astro';

const title = "Cost Per 100 mL Calculator";

let totalCost = "";
let volume = "";
let pricePer100ml = null;

// ✅ Secure form handling with POST + content-type guard
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  totalCost = form.get('totalCost') || "";
  volume = form.get('volume') || "";

  const cost = parseFloat(totalCost);
  const ml = parseFloat(volume);

  if (!isNaN(cost) && !isNaN(ml) && ml > 0) {
    pricePer100ml = (cost / ml) * 100;
  }
}
---

<Layout>
  <!-- 🧴 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">🧴 {title}</h1>
    <p class="text-base">Standardize price comparisons for liquids by converting to cost per 100 mL.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Especially useful for comparing items like shampoo, cleaning products, sauces, or beverages with different container sizes.</p>
  </div>

  <!-- 🔢 Calculator Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="totalCost" class="block font-medium text-gray-800">Total Cost ($):</label>
        <input
          type="number"
          step="any"
          name="totalCost"
          id="totalCost"
          class="w-full p-2 border rounded"
          value={totalCost}
          required
        />
      </div>

      <div>
        <label for="volume" class="block font-medium text-gray-800">Total Volume (mL):</label>
        <input
          type="number"
          step="any"
          name="volume"
          id="volume"
          class="w-full p-2 border rounded"
          value={volume}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Price Per 100 mL
      </button>
    </form>

    {pricePer100ml !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Price Per 100 mL: ${pricePer100ml.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Different bottle sizes can make prices misleading. Converting to a standard 100 mL unit gives you a fair comparison across brands and packages.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Check bulk cleaning supplies and personal care items for real savings.</li>
      <li>Use when comparing prices for oils, drinks, sauces, and similar liquids.</li>
      <li>Smaller containers sometimes cost more per 100 mL than large ones — test it!</li>
    </ul>
  </section>
</Layout>


