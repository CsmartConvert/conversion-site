---
import Layout from '../../layouts/Layout.astro';

const title = "Amps to Watts Converter";

let amps = "";
let volts = "";
let watts = null;

if (
  Astro.request?.method === "POST" &&
  Astro.request.headers.get("content-type")?.includes("application/x-www-form-urlencoded")
) {
  const form = await Astro.request.formData();
  amps = form.get("amps") || "";
  volts = form.get("volts") || "";

  const a = parseFloat(amps);
  const v = parseFloat(volts);

  if (!isNaN(a) && !isNaN(v)) {
    watts = a * v;
  }
}
---

<Layout>
  <!-- 🔌 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">🔌 {title}</h1>
    <p class="text-base">Convert electrical current (amps) and voltage (volts) into power (watts).</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Watts = Amps × Volts. This is useful when sizing electrical loads and appliances.</p>
  </div>

  <!-- 🔢 Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="amps" class="block font-medium text-gray-800">Current (Amps):</label>
        <input
          type="number"
          step="any"
          name="amps"
          id="amps"
          class="w-full p-2 border rounded"
          value={amps}
          required
        />
      </div>

      <div>
        <label for="volts" class="block font-medium text-gray-800">Voltage (Volts):</label>
        <input
          type="number"
          step="any"
          name="volts"
          id="volts"
          class="w-full p-2 border rounded"
          value={volts}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Convert to Watts
      </button>
    </form>

    {watts !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Power: {watts.toFixed(2)} watts
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Knowing how many watts your appliances or circuits use helps prevent overloads and improves efficiency. It's essential for planning solar setups, battery backups, and electrical safety.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Use RMS voltage for AC power unless you're sure it's DC.</li>
      <li>Round up watt values when sizing generators or circuits.</li>
      <li>Double-check units: Volts (V), Amps (A), and Watts (W).</li>
    </ul>
  </section>
</Layout>
