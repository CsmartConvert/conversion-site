---
import Layout from '../../layouts/Layout.astro';

const title = "Tip Calculator";

let billAmount = "";
let tipPercentage = "";
let totalWithTip = null;

if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();
  billAmount = form.get('billAmount') || "";
  tipPercentage = form.get('tipPercentage') || "";

  const bill = parseFloat(billAmount);
  const tip = parseFloat(tipPercentage);

  if (!isNaN(bill) && !isNaN(tip)) {
    const tipAmount = bill * (tip / 100);
    totalWithTip = bill + tipAmount;
  }
}
---

<Layout>
  <!-- 💰 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">💰 {title}</h1>
    <p class="text-base">Quickly calculate your tip and total bill amount.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Tipping around 15–20% is standard for good service in many countries.</p>
  </div>

  <!-- 🔢 Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="billAmount" class="block font-medium text-gray-800">Bill Amount ($):</label>
        <input
          type="number"
          step="any"
          name="billAmount"
          id="billAmount"
          class="w-full p-2 border rounded"
          value={billAmount}
          required
        />
      </div>

      <div>
        <label for="tipPercentage" class="block font-medium text-gray-800">Tip Percentage (%):</label>
        <input
          type="number"
          step="any"
          name="tipPercentage"
          id="tipPercentage"
          class="w-full p-2 border rounded"
          value={tipPercentage}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Tip
      </button>
    </form>

    {totalWithTip !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Total with Tip: ${totalWithTip.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Tipping fairly supports service workers and is a social expectation in many cultures. This calculator helps you quickly decide how much to leave.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Double the tax in the U.S. to estimate a 15–20% tip.</li>
      <li>Round up your total for easier payment.</li>
      <li>Check if gratuity is already included on large parties or international receipts.</li>
    </ul>
  </section>
</Layout>



