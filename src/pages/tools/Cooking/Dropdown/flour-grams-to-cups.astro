---
const pageTitle = 'Flour Grams to Cups Converter';
const pageDesc = 'Convert flour grams to cups with a dropdown for all-purpose, bread, cake, and whole wheat flour.';
const path = '/tools/cooking/dropdown/flour-grams-to-cups';

const ingredientOptions = [
  { name: 'All-purpose Flour', ratio: 0.0078125 },   // ~1g = 0.0078125 cups
  { name: 'Bread Flour', ratio: 0.0075758 },
  { name: 'Cake Flour', ratio: 0.0082645 },
  { name: 'Whole Wheat Flour', ratio: 0.0073529 },
];

let mode = 'gramsToCups';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{pageTitle} | Smart Converter</title>
    <meta name="description" content={pageDesc} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">

      <!-- Header -->
      <header class="bg-gradient-to-r from-purple-600 to-purple-800 text-white p-6 rounded-t-xl shadow">
        <h1 class="text-3xl font-bold">Flour Grams ↔ Cups Converter</h1>
        <p class="text-base mt-2">Choose your flour type and convert grams to cups with precision.</p>
      </header>

      <!-- Seasonal Tip -->
      <div class="mt-6 mb-4 bg-purple-100 dark:bg-purple-800 text-purple-900 dark:text-purple-100 p-4 text-sm rounded text-center">
        💡 <strong>Seasonal Tip:</strong> Baking bread this fall? Use this converter to adjust recipes using different flour types!
      </div>

      <!-- Converter -->
      <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-6 space-y-4">
        <!-- Ingredient Dropdown -->
        <div>
          <label for="ingredient" class="block font-semibold mb-1">Flour Type</label>
          <select id="ingredient" class="w-full p-2 rounded border dark:bg-gray-900 dark:border-gray-600">
            {ingredientOptions.map((item, i) => (
              <option value={i}>{item.name}</option>
            ))}
          </select>
        </div>

        <!-- Switch & Mode -->
        <div class="flex items-center justify-between">
          <p id="modeLabel" class="text-lg font-medium">Convert Grams to Cups</p>
          <button id="switchBtn" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded text-sm">🔁 Switch</button>
        </div>

        <!-- Inputs -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label id="inputLabel" for="inputValue" class="block mb-1 font-medium">Grams</label>
            <input id="inputValue" type="number" step="any" class="w-full p-3 rounded border text-lg dark:bg-gray-900 dark:border-gray-700" />
          </div>
          <div>
            <label id="outputLabel" for="outputValue" class="block mb-1 font-medium">Cups</label>
            <input id="outputValue" type="text" disabled class="w-full p-3 rounded border bg-gray-100 text-gray-500 text-lg dark:bg-gray-800 dark:border-gray-700" />
          </div>
        </div>

        <p id="resultDisplay" class="text-2xl font-bold text-purple-700 text-center mt-4">—</p>
      </div>

      <!-- Conversion Table -->
      <section class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-4">Common Conversions</h2>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
          <li>50g All-purpose Flour ≈ 0.3906 cups</li>
          <li>100g Bread Flour ≈ 0.7576 cups</li>
          <li>75g Cake Flour ≈ 0.6198 cups</li>
          <li>60g Whole Wheat Flour ≈ 0.4412 cups</li>
          <li>120g All-purpose Flour ≈ 0.9375 cups</li>
        </ul>
      </section>

      <!-- Learn More -->
      <section class="prose dark:prose-invert">
        <h2>Why Different Flours Matter</h2>
        <p>Flour types have different densities, which affects how much volume a given weight will occupy. Cake flour is lighter and fluffier than whole wheat flour, for example.</p>

        <h2>Measuring Tips</h2>
        <ul>
          <li>Spoon flour into the measuring cup and level it off (don’t scoop directly).</li>
          <li>Use a kitchen scale when precision is critical (e.g., baking).</li>
          <li>Always select the correct flour type from the dropdown.</li>
        </ul>
      </section>

      <!-- FAQs -->
      <section class="prose dark:prose-invert">
        <h2>FAQs</h2>

        <h3>Can I use this for gluten-free flours?</h3>
        <p>Not directly — gluten-free flours have different densities. Use a specific converter for that.</p>

        <h3>Is this based on sifted flour?</h3>
        <p>No, these are for unsifted flour weights. Sifted flour will have slightly different results.</p>

        <h3>Can I convert cups to grams too?</h3>
        <p>Yes! Just hit the switch button to reverse the conversion mode.</p>
      </section>

      <!-- Structured Data -->
      <script type="application/ld+json" is:inline>
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Flour Grams to Cups Converter",
          "description": "Convert flour grams to cups based on flour type: all-purpose, bread, cake, or whole wheat.",
          "url": "https://yourdomain.com/tools/cooking/dropdown/flour-grams-to-cups"
        }
      </script>
    </main>

    <script is:inline>
      const input = document.getElementById('inputValue');
      const output = document.getElementById('outputValue');
      const resultDisplay = document.getElementById('resultDisplay');
      const switchBtn = document.getElementById('switchBtn');
      const ingredientSelect = document.getElementById('ingredient');

      const inputLabel = document.getElementById('inputLabel');
      const outputLabel = document.getElementById('outputLabel');
      const modeLabel = document.getElementById('modeLabel');

      const ingredientRatios = [
        { name: 'All-purpose Flour', ratio: 0.0078125 },
        { name: 'Bread Flour', ratio: 0.0075758 },
        { name: 'Cake Flour', ratio: 0.0082645 },
        { name: 'Whole Wheat Flour', ratio: 0.0073529 },
      ];

      let currentRatio = ingredientRatios[0].ratio;
      let mode = 'gramsToCups';

      const convert = () => {
        const val = parseFloat(input.value);
        if (isNaN(val)) {
          output.value = '';
          resultDisplay.textContent = '—';
          return;
        }

        let result = mode === 'gramsToCups'
          ? val * currentRatio
          : val / currentRatio;

        const fromUnit = mode === 'gramsToCups' ? 'g' : 'cups';
        const toUnit = mode === 'gramsToCups' ? 'cups' : 'g';

        output.value = result.toFixed(4);
        resultDisplay.textContent = `${val} ${fromUnit} = ${result.toFixed(4)} ${toUnit}`;
      };

      input.addEventListener('input', convert);

      switchBtn.addEventListener('click', () => {
        mode = mode === 'gramsToCups' ? 'cupsToGrams' : 'gramsToCups';

        if (mode === 'gramsToCups') {
          inputLabel.textContent = 'Grams';
          outputLabel.textContent = 'Cups';
          modeLabel.textContent = 'Convert Grams to Cups';
        } else {
          inputLabel.textContent = 'Cups';
          outputLabel.textContent = 'Grams';
          modeLabel.textContent = 'Convert Cups to Grams';
        }

        input.value = '';
        output.value = '';
        resultDisplay.textContent = '—';
      });

      ingredientSelect.addEventListener('change', (e) => {
        const idx = parseInt(e.target.value);
        currentRatio = ingredientRatios[idx].ratio;
        convert();
      });
    </script>
  </body>
</html>
