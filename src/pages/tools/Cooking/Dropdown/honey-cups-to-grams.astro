---
const pageTitle = 'Honey Cups to Grams Converter';
const pageDesc = 'Convert cups to grams of honey and vice versa using this fast kitchen calculator.';
const path = '/tools/cooking/dropdown/honey-cups-to-grams';

const ingredientOptions = [
  { name: 'Honey', ratio: 340 }, // 1 cup honey ≈ 340g
];

let mode = 'cupsToGrams';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{pageTitle} | Smart Converter</title>
    <meta name="description" content={pageDesc} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">

      <!-- Header -->
      <header class="bg-gradient-to-r from-purple-600 to-purple-800 text-white p-6 rounded-t-xl shadow">
        <h1 class="text-3xl font-bold">Honey Cups ↔ Grams Converter</h1>
        <p class="text-base mt-2">Convert honey between cups and grams using accurate kitchen math.</p>
      </header>

      <!-- Seasonal Tip -->
      <div class="mt-6 mb-4 bg-purple-100 dark:bg-purple-800 text-purple-900 dark:text-purple-100 p-4 text-sm rounded text-center">
        💡 <strong>Seasonal Tip:</strong> Great for adjusting sweetener amounts in holiday baking and glazes!
      </div>

      <!-- Converter -->
      <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-6 space-y-4">

        <!-- Ingredient Dropdown -->
        <div>
          <label for="ingredient" class="block font-semibold mb-1">Ingredient Type</label>
          <select id="ingredient" class="w-full p-2 rounded border dark:bg-gray-900 dark:border-gray-600">
            {ingredientOptions.map((item, i) => (
              <option value={i}>{item.name}</option>
            ))}
          </select>
        </div>

        <!-- Mode Switch -->
        <div class="flex items-center justify-between">
          <p id="modeLabel" class="text-lg font-medium">Convert Cups to Grams</p>
          <button id="switchBtn" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded text-sm">🔁 Switch</button>
        </div>

        <!-- Input Fields -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label id="inputLabel" for="inputValue" class="block mb-1 font-medium">Cups</label>
            <input id="inputValue" type="number" step="any" class="w-full p-3 rounded border text-lg dark:bg-gray-900 dark:border-gray-700" />
          </div>
          <div>
            <label id="outputLabel" for="outputValue" class="block mb-1 font-medium">Grams</label>
            <input id="outputValue" type="text" disabled class="w-full p-3 rounded border bg-gray-100 text-gray-500 text-lg dark:bg-gray-800 dark:border-gray-700" />
          </div>
        </div>

        <p id="resultDisplay" class="text-2xl font-bold text-purple-700 text-center mt-4">—</p>
      </div>

      <!-- Conversion Table -->
      <section class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-4">Common Conversions</h2>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
          <li>¼ cup honey ≈ 85g</li>
          <li>½ cup honey ≈ 170g</li>
          <li>1 cup honey ≈ 340g</li>
          <li>1.5 cups honey ≈ 510g</li>
          <li>2 cups honey ≈ 680g</li>
        </ul>
      </section>

      <!-- Learn More -->
      <section class="prose dark:prose-invert">
        <h2>How Much Does a Cup of Honey Weigh?</h2>
        <p>Honey is denser than water or flour. One US cup of honey weighs approximately 340 grams. This makes it important to use specific converters rather than general volume-to-weight tools.</p>

        <h2>Kitchen Tips for Measuring Honey</h2>
        <ul>
          <li>Use a scale for best precision when baking.</li>
          <li>Coat measuring cups with oil to reduce stickiness.</li>
          <li>Always select "Honey" from the dropdown above to ensure accuracy.</li>
        </ul>
      </section>

      <!-- FAQs -->
      <section class="prose dark:prose-invert">
        <h2>FAQs</h2>

        <h3>How many grams in a tablespoon of honey?</h3>
        <p>There are about 21.3 grams in one US tablespoon of honey.</p>

        <h3>Is honey heavier than water?</h3>
        <p>Yes. Honey is denser than water, so a cup of honey weighs more than a cup of water.</p>

        <h3>Is this calculator based on US cups?</h3>
        <p>Yes. This tool uses the US cup standard of 240 mL.</p>
      </section>

      <!-- JSON-LD Structured Data -->
      <script type="application/ld+json" is:inline>
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Honey Cups to Grams Converter",
          "description": "Convert honey between cups and grams easily. Useful for cooking and baking with sweeteners.",
          "url": "https://yourdomain.com/tools/cooking/dropdown/honey-cups-to-grams"
        }
      </script>
    </main>

    <!-- JS Logic -->
    <script is:inline>
      const input = document.getElementById('inputValue');
      const output = document.getElementById('outputValue');
      const resultDisplay = document.getElementById('resultDisplay');
      const switchBtn = document.getElementById('switchBtn');
      const ingredientSelect = document.getElementById('ingredient');

      const inputLabel = document.getElementById('inputLabel');
      const outputLabel = document.getElementById('outputLabel');
      const modeLabel = document.getElementById('modeLabel');

      const ingredientRatios = [
        { name: 'Honey', ratio: 340 },
      ];

      let currentRatio = ingredientRatios[0].ratio;
      let mode = 'cupsToGrams';

      const convert = () => {
        const val = parseFloat(input.value);
        if (isNaN(val)) {
          output.value = '';
          resultDisplay.textContent = '—';
          return;
        }

        let result = mode === 'cupsToGrams'
          ? val * currentRatio
          : val / currentRatio;

        const fromUnit = mode === 'cupsToGrams' ? 'cups' : 'g';
        const toUnit = mode === 'cupsToGrams' ? 'g' : 'cups';

        output.value = result.toFixed(4);
        resultDisplay.textContent = `${val} ${fromUnit} = ${result.toFixed(4)} ${toUnit}`;
      };

      input.addEventListener('input', convert);

      switchBtn.addEventListener('click', () => {
        mode = mode === 'cupsToGrams' ? 'gramsToCups' : 'cupsToGrams';

        if (mode === 'cupsToGrams') {
          inputLabel.textContent = 'Cups';
          outputLabel.textContent = 'Grams';
          modeLabel.textContent = 'Convert Cups to Grams';
        } else {
          inputLabel.textContent = 'Grams';
          outputLabel.textContent = 'Cups';
          modeLabel.textContent = 'Convert Grams to Cups';
        }

        input.value = '';
        output.value = '';
        resultDisplay.textContent = '—';
      });

      ingredientSelect.addEventListener('change', (e) => {
        const idx = parseInt(e.target.value);
        currentRatio = ingredientRatios[idx].ratio;
        convert();
      });
    </script>
  </body>
</html>
