---
const pageTitle = 'Salt Grams to Cups Converter';
const pageDesc = 'Convert grams of salt to cups using a dropdown for table salt, kosher salt, and sea salt.';
const path = '/tools/cooking/dropdown/salt-grams-to-cups';

const ingredientOptions = [
  { name: 'Table Salt', ratio: 0.0585 },     // 1g ≈ 0.0585 cups
  { name: 'Kosher Salt', ratio: 0.071 },     // coarser, lighter
  { name: 'Sea Salt', ratio: 0.065 },
];

let mode = 'gramsToCups';
---
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{pageTitle} | Smart Converter</title>
    <meta name="description" content={pageDesc} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">

      <!-- Header -->
      <header class="bg-gradient-to-r from-purple-600 to-purple-800 text-white p-6 rounded-t-xl shadow">
        <h1 class="text-3xl font-bold">Salt Grams ↔ Cups Converter</h1>
        <p class="text-base mt-2">Convert grams of salt to cups with a quick switch and ingredient selector.</p>
      </header>

      <!-- Seasonal Tip -->
      <div class="mt-6 mb-4 bg-purple-100 dark:bg-purple-800 text-purple-900 dark:text-purple-100 p-4 text-sm rounded text-center">
        💡 <strong>Seasonal Tip:</strong> Perfect for scaling savory recipes when switching salt types.
      </div>

      <!-- Converter -->
      <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-6 space-y-4">
        <div>
          <label for="ingredient" class="block font-semibold mb-1">Salt Type</label>
          <select id="ingredient" class="w-full p-2 rounded border dark:bg-gray-900 dark:border-gray-600">
            {ingredientOptions.map((item, i) => (
              <option value={i}>{item.name}</option>
            ))}
          </select>
        </div>

        <div class="flex items-center justify-between">
          <p id="modeLabel" class="text-lg font-medium">Convert Grams to Cups</p>
          <button id="switchBtn" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded text-sm">🔁 Switch</button>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label id="inputLabel" for="inputValue" class="block mb-1 font-medium">Grams</label>
            <input id="inputValue" type="number" step="any" class="w-full p-3 rounded border text-lg dark:bg-gray-900 dark:border-gray-700" />
          </div>
          <div>
            <label id="outputLabel" for="outputValue" class="block mb-1 font-medium">Cups</label>
            <input id="outputValue" type="text" disabled class="w-full p-3 rounded border bg-gray-100 text-gray-500 text-lg dark:bg-gray-800 dark:border-gray-700" />
          </div>
        </div>

        <p id="resultDisplay" class="text-2xl font-bold text-purple-700 text-center mt-4">—</p>
      </div>

      <!-- Conversion Table -->
      <section class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-4">Common Salt Conversions</h2>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
          <li>5g Table Salt ≈ 0.293 cups</li>
          <li>10g Kosher Salt ≈ 0.71 cups</li>
          <li>15g Sea Salt ≈ 0.975 cups</li>
          <li>30g Kosher Salt ≈ 2.13 cups</li>
          <li>50g Table Salt ≈ 2.93 cups</li>
        </ul>
      </section>

      <!-- Learn More -->
      <section class="prose dark:prose-invert">
        <h2>Why Salt Type Matters</h2>
        <p>Different salts have different grain sizes and densities. This affects how much volume (cups) the same weight (grams) of salt takes up. Kosher salt is flakier and less dense than table salt, for example.</p>

        <h2>Tips for Measuring Salt</h2>
        <ul>
          <li>Use a digital scale for accuracy, especially when baking or curing.</li>
          <li>Stick with one type of salt per recipe or convert accordingly.</li>
          <li>Conversions are rounded to four decimal places.</li>
        </ul>
      </section>

      <!-- FAQs -->
      <section class="prose dark:prose-invert">
        <h2>FAQs</h2>

        <h3>Is there a big difference between salt types?</h3>
        <p>Yes — by volume. Kosher salt can take up more space than table salt, even if the weight is the same.</p>

        <h3>Can I use this tool for flavored salts?</h3>
        <p>Use with caution. Flavored salts may have different densities depending on additives and texture.</p>
      </section>

      <!-- Structured Data -->
      <script type="application/ld+json" is:inline>
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Salt Grams to Cups Converter",
          "description": "Convert grams of salt to cups using a dropdown for table salt, kosher salt, and sea salt.",
          "url": "https://yourdomain.com/tools/cooking/dropdown/salt-grams-to-cups"
        }
      </script>
    </main>

    <!-- Converter Script -->
    <script is:inline>
      const input = document.getElementById('inputValue');
      const output = document.getElementById('outputValue');
      const resultDisplay = document.getElementById('resultDisplay');
      const switchBtn = document.getElementById('switchBtn');
      const ingredientSelect = document.getElementById('ingredient');

      const inputLabel = document.getElementById('inputLabel');
      const outputLabel = document.getElementById('outputLabel');
      const modeLabel = document.getElementById('modeLabel');

      const ingredientRatios = [
        { name: 'Table Salt', ratio: 0.0585 },
        { name: 'Kosher Salt', ratio: 0.071 },
        { name: 'Sea Salt', ratio: 0.065 },
      ];

      let currentRatio = ingredientRatios[0].ratio;
      let mode = 'gramsToCups';

      const convert = () => {
        const val = parseFloat(input.value);
        if (isNaN(val)) {
          output.value = '';
          resultDisplay.textContent = '—';
          return;
        }

        let result = mode === 'gramsToCups'
          ? val * currentRatio
          : val / currentRatio;

        const fromUnit = mode === 'gramsToCups' ? 'g' : 'cups';
        const toUnit = mode === 'gramsToCups' ? 'cups' : 'g';

        output.value = result.toFixed(4);
        resultDisplay.textContent = `${val} ${fromUnit} = ${result.toFixed(4)} ${toUnit}`;
      };

      input.addEventListener('input', convert);

      switchBtn.addEventListener('click', () => {
        mode = mode === 'gramsToCups' ? 'cupsToGrams' : 'gramsToCups';

        if (mode === 'gramsToCups') {
          inputLabel.textContent = 'Grams';
          outputLabel.textContent = 'Cups';
          modeLabel.textContent = 'Convert Grams to Cups';
        } else {
          inputLabel.textContent = 'Cups';
          outputLabel.textContent = 'Grams';
          modeLabel.textContent = 'Convert Cups to Grams';
        }

        input.value = '';
        output.value = '';
        resultDisplay.textContent = '—';
      });

      ingredientSelect.addEventListener('change', (e) => {
        const idx = parseInt(e.target.value);
        currentRatio = ingredientRatios[idx].ratio;
        convert();
      });
    </script>
  </body>
</html>
