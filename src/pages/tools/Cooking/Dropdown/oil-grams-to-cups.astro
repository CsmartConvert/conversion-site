---
const pageTitle = 'Oil Grams to Cups Converter';
const pageDesc = 'Convert grams of oil to cups with a dropdown for olive oil, vegetable oil, coconut oil, and more.';
const path = '/tools/cooking/dropdown/oil-grams-to-cups';

const ingredientOptions = [
  { name: 'Olive Oil', ratio: 0.001084 },       // 1g ≈ 0.001084 cups
  { name: 'Vegetable Oil', ratio: 0.001085 },
  { name: 'Coconut Oil', ratio: 0.00103 },
  { name: 'Canola Oil', ratio: 0.001087 },
];

let mode = 'gramsToCups';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{pageTitle} | Smart Converter</title>
    <meta name="description" content={pageDesc} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">

      <!-- Header -->
      <header class="bg-gradient-to-r from-purple-600 to-purple-800 text-white p-6 rounded-t-xl shadow">
        <h1 class="text-3xl font-bold">Oil Grams ↔ Cups Converter</h1>
        <p class="text-base mt-2">Convert grams of oil to cups by type — olive, vegetable, coconut, and more.</p>
      </header>

      <!-- Seasonal Tip -->
      <div class="mt-6 mb-4 bg-purple-100 dark:bg-purple-800 text-purple-900 dark:text-purple-100 p-4 text-sm rounded text-center">
        💡 <strong>Seasonal Tip:</strong> Handy for sautéing, frying, and healthy salad dressings.
      </div>

      <!-- Converter -->
      <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-6 space-y-4">
        <div>
          <label for="ingredient" class="block font-semibold mb-1">Oil Type</label>
          <select id="ingredient" class="w-full p-2 rounded border dark:bg-gray-900 dark:border-gray-600">
            {ingredientOptions.map((item, i) => (
              <option value={i}>{item.name}</option>
            ))}
          </select>
        </div>

        <div class="flex items-center justify-between">
          <p id="modeLabel" class="text-lg font-medium">Convert Grams to Cups</p>
          <button id="switchBtn" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded text-sm">🔁 Switch</button>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label id="inputLabel" for="inputValue" class="block mb-1 font-medium">Grams</label>
            <input id="inputValue" type="number" step="any" class="w-full p-3 rounded border text-lg dark:bg-gray-900 dark:border-gray-700" />
          </div>
          <div>
            <label id="outputLabel" for="outputValue" class="block mb-1 font-medium">Cups</label>
            <input id="outputValue" type="text" disabled class="w-full p-3 rounded border bg-gray-100 text-gray-500 text-lg dark:bg-gray-800 dark:border-gray-700" />
          </div>
        </div>

        <p id="resultDisplay" class="text-2xl font-bold text-purple-700 text-center mt-4">—</p>
      </div>

      <!-- Conversion Table -->
      <section class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-4">Common Conversions</h2>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
          <li>100g Olive Oil ≈ 0.1084 cups</li>
          <li>100g Vegetable Oil ≈ 0.1085 cups</li>
          <li>100g Coconut Oil ≈ 0.103 cups</li>
          <li>100g Canola Oil ≈ 0.1087 cups</li>
        </ul>
      </section>

      <!-- Learn More -->
      <section class="prose dark:prose-invert">
        <h2>Why Oil Type Matters</h2>
        <p>Oils vary in density. Coconut oil is thicker than vegetable oil, and using the right type ensures accurate conversions. This tool accounts for that by offering a dropdown menu.</p>

        <h2>Tips for Cooking with Oil</h2>
        <ul>
          <li>Olive oil is great for sautéing and salad dressings.</li>
          <li>Vegetable oil is neutral and ideal for baking and frying.</li>
          <li>Use a kitchen scale when possible for precise recipes.</li>
        </ul>
      </section>

      <!-- FAQs -->
      <section class="prose dark:prose-invert">
        <h2>FAQs</h2>

        <h3>Can I use this for melted butter or shortening?</h3>
        <p>Not directly. This tool is specifically designed for oils. Use the butter converter for melted butter.</p>

        <h3>Is 1 cup a US standard?</h3>
        <p>Yes, this tool uses 1 US cup = 240 mL as the standard volume.</p>
      </section>

      <!-- Structured Data -->
      <script type="application/ld+json" is:inline>
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Oil Grams to Cups Converter",
          "description": "Convert grams of oil to cups by selecting olive oil, vegetable oil, coconut oil, or canola oil.",
          "url": "https://yourdomain.com/tools/cooking/dropdown/oil-grams-to-cups"
        }
      </script>
    </main>

    <script is:inline>
      const input = document.getElementById('inputValue');
      const output = document.getElementById('outputValue');
      const resultDisplay = document.getElementById('resultDisplay');
      const switchBtn = document.getElementById('switchBtn');
      const ingredientSelect = document.getElementById('ingredient');

      const inputLabel = document.getElementById('inputLabel');
      const outputLabel = document.getElementById('outputLabel');
      const modeLabel = document.getElementById('modeLabel');

      const ingredientRatios = [
        { name: 'Olive Oil', ratio: 0.001084 },
        { name: 'Vegetable Oil', ratio: 0.001085 },
        { name: 'Coconut Oil', ratio: 0.00103 },
        { name: 'Canola Oil', ratio: 0.001087 },
      ];

      let currentRatio = ingredientRatios[0].ratio;
      let mode = 'gramsToCups';

      const convert = () => {
        const val = parseFloat(input.value);
        if (isNaN(val)) {
          output.value = '';
          resultDisplay.textContent = '—';
          return;
        }

        let result = mode === 'gramsToCups'
          ? val * currentRatio
          : val / currentRatio;

        const fromUnit = mode === 'gramsToCups' ? 'g' : 'cups';
        const toUnit = mode === 'gramsToCups' ? 'cups' : 'g';

        output.value = result.toFixed(4);
        resultDisplay.textContent = `${val} ${fromUnit} = ${result.toFixed(4)} ${toUnit}`;
      };

      input.addEventListener('input', convert);

      switchBtn.addEventListener('click', () => {
        mode = mode === 'gramsToCups' ? 'cupsToGrams' : 'gramsToCups';

        if (mode === 'gramsToCups') {
          inputLabel.textContent = 'Grams';
          outputLabel.textContent = 'Cups';
          modeLabel.textContent = 'Convert Grams to Cups';
        } else {
          inputLabel.textContent = 'Cups';
          outputLabel.textContent = 'Grams';
          modeLabel.textContent = 'Convert Cups to Grams';
        }

        input.value = '';
        output.value = '';
        resultDisplay.textContent = '—';
      });

      ingredientSelect.addEventListener('change', (e) => {
        const idx = parseInt(e.target.value);
        currentRatio = ingredientRatios[idx].ratio;
        convert();
      });
    </script>
  </body>
</html>
