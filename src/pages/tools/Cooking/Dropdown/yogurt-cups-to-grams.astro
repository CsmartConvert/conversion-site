---
const pageTitle = 'Yogurt Cups to Grams Converter';
const pageDesc = 'Convert cups of yogurt to grams (or grams to cups) using specific yogurt types for accurate kitchen measurements.';
const path = '/tools/cooking/dropdown/yogurt-cups-to-grams';

const ingredientOptions = [
  { name: 'Plain Yogurt (Whole Milk)', ratio: 245 },
  { name: 'Greek Yogurt (Nonfat)', ratio: 260 },
  { name: 'Greek Yogurt (Whole Milk)', ratio: 240 },
  { name: 'Low-fat Yogurt', ratio: 235 },
];

let mode = 'cupsToGrams';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{pageTitle} | Smart Converter</title>
    <meta name="description" content={pageDesc} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-gray-100">
    <main class="max-w-3xl mx-auto px-4 py-8 space-y-8">

      <!-- Header -->
      <header class="bg-gradient-to-r from-purple-600 to-purple-800 text-white p-6 rounded-t-xl shadow">
        <h1 class="text-3xl font-bold">Yogurt Cups ↔ Grams Converter</h1>
        <p class="text-base mt-2">Convert cups of yogurt to grams based on the yogurt type.</p>
      </header>

      <!-- Seasonal Tip -->
      <div class="mt-6 mb-4 bg-purple-100 dark:bg-purple-800 text-purple-900 dark:text-purple-100 p-4 text-sm rounded text-center">
        🥣 <strong>Seasonal Tip:</strong> Great for prepping smoothies or portioning breakfast bowls with exact measurements!
      </div>

      <!-- Converter -->
      <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-6 space-y-4">
        <div>
          <label for="ingredient" class="block font-semibold mb-1">Yogurt Type</label>
          <select id="ingredient" class="w-full p-2 rounded border dark:bg-gray-900 dark:border-gray-600">
            {ingredientOptions.map((item, i) => (
              <option value={i}>{item.name}</option>
            ))}
          </select>
        </div>

        <div class="flex items-center justify-between">
          <p id="modeLabel" class="text-lg font-medium">Convert Cups to Grams</p>
          <button id="switchBtn" class="bg-purple-700 hover:bg-purple-800 text-white px-4 py-2 rounded text-sm">🔁 Switch</button>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label id="inputLabel" for="inputValue" class="block mb-1 font-medium">Cups</label>
            <input id="inputValue" type="number" step="any" class="w-full p-3 rounded border text-lg dark:bg-gray-900 dark:border-gray-700" />
          </div>
          <div>
            <label id="outputLabel" for="outputValue" class="block mb-1 font-medium">Grams</label>
            <input id="outputValue" type="text" disabled class="w-full p-3 rounded border bg-gray-100 text-gray-500 text-lg dark:bg-gray-800 dark:border-gray-700" />
          </div>
        </div>

        <p id="resultDisplay" class="text-2xl font-bold text-purple-700 text-center mt-4">—</p>
      </div>

      <!-- Quick Reference -->
      <section class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-4">Quick Reference</h2>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
          <li>1 cup Plain Yogurt ≈ 245g</li>
          <li>1 cup Greek Yogurt (Nonfat) ≈ 260g</li>
          <li>1 cup Greek Yogurt (Whole Milk) ≈ 240g</li>
          <li>1 cup Low-fat Yogurt ≈ 235g</li>
        </ul>
      </section>

      <!-- Learn More -->
      <section class="prose dark:prose-invert">
        <h2>Why Yogurt Type Matters</h2>
        <p>Different types of yogurt have varying densities due to fat content and straining. This tool ensures accurate conversions whether you’re using Greek, plain, or low-fat yogurt.</p>

        <h2>Measuring Tips</h2>
        <ul>
          <li>Use a food scale for most accurate weight-based measurements.</li>
          <li>Level your measuring cup when portioning yogurt.</li>
          <li>Select the correct yogurt type from the dropdown to match your recipe.</li>
        </ul>
      </section>

      <!-- FAQs -->
      <section class="prose dark:prose-invert">
        <h2>FAQs</h2>

        <h3>Is this converter based on US cup size?</h3>
        <p>Yes, all cup-to-gram conversions use the standard US cup (1 cup = 240 mL).</p>

        <h3>Can I use this tool for flavored yogurt?</h3>
        <p>Yes, but density may vary slightly due to additives. Use plain or Greek yogurt options for closest accuracy.</p>

        <h3>What’s the difference between Greek and regular yogurt?</h3>
        <p>Greek yogurt is thicker and more concentrated, so it weighs more per cup than regular yogurt.</p>
      </section>

      <!-- Structured Data -->
      <script type="application/ld+json" is:inline>
        {
          "@context": "https://schema.org",
          "@type": "WebPage",
          "name": "Yogurt Cups to Grams Converter",
          "description": "Convert yogurt between cups and grams using options like plain, Greek, and low-fat yogurt for precise recipe measurements.",
          "url": "https://yourdomain.com/tools/cooking/dropdown/yogurt-cups-to-grams"
        }
      </script>

      <!-- Inline Script -->
      <script is:inline>
        const input = document.getElementById('inputValue');
        const output = document.getElementById('outputValue');
        const resultDisplay = document.getElementById('resultDisplay');
        const switchBtn = document.getElementById('switchBtn');
        const ingredientSelect = document.getElementById('ingredient');

        const inputLabel = document.getElementById('inputLabel');
        const outputLabel = document.getElementById('outputLabel');
        const modeLabel = document.getElementById('modeLabel');

        const ingredientRatios = [
          { name: 'Plain Yogurt (Whole Milk)', ratio: 245 },
          { name: 'Greek Yogurt (Nonfat)', ratio: 260 },
          { name: 'Greek Yogurt (Whole Milk)', ratio: 240 },
          { name: 'Low-fat Yogurt', ratio: 235 },
        ];

        let currentRatio = ingredientRatios[0].ratio;
        let mode = 'cupsToGrams';

        const convert = () => {
          const val = parseFloat(input.value);
          if (isNaN(val)) {
            output.value = '';
            resultDisplay.textContent = '—';
            return;
          }

          let result = mode === 'cupsToGrams' ? val * currentRatio : val / currentRatio;
          const fromUnit = mode === 'cupsToGrams' ? 'cups' : 'g';
          const toUnit = mode === 'cupsToGrams' ? 'g' : 'cups';

          output.value = result.toFixed(2);
          resultDisplay.textContent = `${val} ${fromUnit} = ${result.toFixed(2)} ${toUnit}`;
        };

        input.addEventListener('input', convert);

        switchBtn.addEventListener('click', () => {
          mode = mode === 'cupsToGrams' ? 'gramsToCups' : 'cupsToGrams';

          if (mode === 'cupsToGrams') {
            inputLabel.textContent = 'Cups';
            outputLabel.textContent = 'Grams';
            modeLabel.textContent = 'Convert Cups to Grams';
          } else {
            inputLabel.textContent = 'Grams';
            outputLabel.textContent = 'Cups';
            modeLabel.textContent = 'Convert Grams to Cups';
          }

          input.value = '';
          output.value = '';
          resultDisplay.textContent = '—';
        });

        ingredientSelect.addEventListener('change', (e) => {
          const idx = parseInt(e.target.value);
          currentRatio = ingredientRatios[idx].ratio;
          convert();
        });
      </script>
    </main>
  </body>
</html>
