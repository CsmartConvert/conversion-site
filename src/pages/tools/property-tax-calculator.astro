---
import Layout from '../../layouts/Layout.astro';

const title = "Property Tax Calculator";

let homeValue = "";
let taxRate = "";
let annualTax = null;

// ✅ Proper SSR form guard
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  homeValue = form.get('homeValue') || "";
  taxRate = form.get('taxRate') || "";

  const value = parseFloat(homeValue);
  const rate = parseFloat(taxRate);

  if (!isNaN(value) && !isNaN(rate)) {
    annualTax = value * (rate / 100);
  }
}
---

<Layout>
  <!-- 🧾 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">🧾 {title}</h1>
    <p class="text-base">Estimate your yearly property tax based on home value and local tax rate.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Property tax rates vary by location. Use your county’s published rate or a recent tax bill for the most accurate result.</p>
  </div>

  <!-- 🔢 Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="homeValue" class="block font-medium text-gray-800">Home Value ($):</label>
        <input
          type="number"
          step="any"
          name="homeValue"
          id="homeValue"
          class="w-full p-2 border rounded"
          value={homeValue}
          required
        />
      </div>

      <div>
        <label for="taxRate" class="block font-medium text-gray-800">Property Tax Rate (%):</label>
        <input
          type="number"
          step="any"
          name="taxRate"
          id="taxRate"
          class="w-full p-2 border rounded"
          value={taxRate}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Property Tax
      </button>
    </form>

    {annualTax !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Estimated Annual Property Tax: ${annualTax.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Property taxes can add thousands of dollars per year to the cost of homeownership. Understanding your estimated bill helps with budgeting and affordability.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Rates are typically applied to assessed value, which may differ from market value.</li>
      <li>Check for exemptions (like homestead or senior discounts) in your area.</li>
      <li>Property taxes often increase over time — plan for inflation or millage changes.</li>
    </ul>
  </section>
</Layout>


