---
import Layout from '../../layouts/Layout.astro';

const title = "Sales Tax Calculator";

// Default values
let amount = "";
let taxRate = "";
let totalWithTax = null;

// ✅ Correct POST guard using content-type check
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  amount = form.get('amount') || "";
  taxRate = form.get('taxRate') || "";

  const amountNum = parseFloat(amount);
  const taxRateNum = parseFloat(taxRate);

  if (!isNaN(amountNum) && !isNaN(taxRateNum)) {
    const tax = amountNum * (taxRateNum / 100);
    totalWithTax = amountNum + tax;
  }
}
---

<Layout>
  <!-- 🧾 Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">🧾 {title}</h1>
    <p class="text-base">Find out the total cost of your purchase after sales tax.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Knowing the final price helps you avoid surprises at checkout, especially in states with high tax rates!</p>
  </div>

  <!-- 🔢 Calculator Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="amount" class="block font-medium text-gray-800">Purchase Amount ($):</label>
        <input
          type="number"
          step="any"
          name="amount"
          id="amount"
          class="w-full p-2 border rounded"
          value={amount}
          required
        />
      </div>

      <div>
        <label for="taxRate" class="block font-medium text-gray-800">Sales Tax Rate (%):</label>
        <input
          type="number"
          step="any"
          name="taxRate"
          id="taxRate"
          class="w-full p-2 border rounded"
          value={taxRate}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Total
      </button>
    </form>

    {totalWithTax !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Total with Tax: ${totalWithTax.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Sales tax varies by state and region — some places charge over 10%! Accurately calculating your final price helps you budget better, especially for large purchases.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Check if the price already includes tax — especially when shopping online.</li>
      <li>Some items (like groceries or clothing) may be tax-exempt depending on where you live.</li>
      <li>Use this tool when budgeting for big purchases like electronics or appliances.</li>
    </ul>
  </section>
</Layout>







