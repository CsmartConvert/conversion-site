---
import Layout from '../../layouts/Layout.astro';

const title = "Watt to Kilowatt Converter";

let watts = "";
let kilowatts = null;

// ✅ Server-safe form handler
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  watts = form.get('watts') || "";
  const wattValue = parseFloat(watts);

  if (!isNaN(wattValue)) {
    kilowatts = wattValue / 1000;
  }
}
---

<Layout>
  <!-- ⚡️ Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">⚡️ {title}</h1>
    <p class="text-base">Convert power from watts (W) to kilowatts (kW) quickly and accurately.</p>
  </section>

  <!-- 💡 Tip Box -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Divide watts by 1,000 to get kilowatts — ideal for understanding appliance energy usage or calculating electricity costs.</p>
  </div>

  <!-- 🔢 Form Section -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="watts" class="block font-medium text-gray-800">Watts (W):</label>
        <input
          type="number"
          step="any"
          name="watts"
          id="watts"
          class="w-full p-2 border rounded"
          value={watts}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Convert to Kilowatts
      </button>
    </form>

    {kilowatts !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        {watts} W = {kilowatts.toFixed(3)} kW
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Many appliances are labeled in watts, but your energy bill is measured in kilowatts. Understanding both units helps you estimate usage and manage costs more effectively.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>1 kilowatt = 1,000 watts — simply divide by 1,000.</li>
      <li>Useful when calculating monthly electricity usage or comparing appliance efficiency.</li>
      <li>Look for energy usage in kilowatt-hours (kWh) on your utility bill.</li>
    </ul>
  </section>
</Layout>
