---
import Layout from '../../layouts/Layout.astro';

const title = "Price Per 100g Calculator";

let totalCost = "";
let weight = "";
let pricePer100g = null;

// ✅ SSR form guard with proper content-type
if (
  Astro.request?.method === 'POST' &&
  Astro.request.headers.get('content-type')?.includes('application/x-www-form-urlencoded')
) {
  const form = await Astro.request.formData();

  totalCost = form.get('totalCost') || "";
  weight = form.get('weight') || "";

  const cost = parseFloat(totalCost);
  const grams = parseFloat(weight);

  if (!isNaN(cost) && !isNaN(grams) && grams > 0) {
    pricePer100g = (cost / grams) * 100;
  }
}
---

<Layout>
  <!-- ⚖️ Header -->
  <section class="max-w-3xl mx-auto bg-gradient-to-r from-blue-600 to-blue-800 text-white py-6 px-4 md:py-8 mb-8 shadow text-center rounded-md">
    <h1 class="text-3xl font-bold mb-2">⚖️ {title}</h1>
    <p class="text-base">Compare grocery prices fairly by converting to cost per 100 grams.</p>
  </section>

  <!-- 💡 Tip -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6 max-w-3xl mx-auto">
    <p class="text-blue-700"><strong>💡 Tip:</strong> Use this when comparing different package sizes — it standardizes pricing for smarter grocery shopping.</p>
  </div>

  <!-- 🔢 Form -->
  <section class="bg-white border border-gray-200 p-6 rounded-lg shadow max-w-2xl mx-auto mb-10">
    <form method="POST" enctype="application/x-www-form-urlencoded" class="space-y-6">
      <div>
        <label for="totalCost" class="block font-medium text-gray-800">Total Cost ($):</label>
        <input
          type="number"
          step="any"
          name="totalCost"
          id="totalCost"
          class="w-full p-2 border rounded"
          value={totalCost}
          required
        />
      </div>

      <div>
        <label for="weight" class="block font-medium text-gray-800">Total Weight (grams):</label>
        <input
          type="number"
          step="any"
          name="weight"
          id="weight"
          class="w-full p-2 border rounded"
          value={weight}
          required
        />
      </div>

      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded font-semibold w-full">
        Calculate Price Per 100g
      </button>
    </form>

    {pricePer100g !== null && (
      <div class="mt-6 bg-green-50 border border-green-300 text-green-800 font-semibold p-4 rounded text-center">
        Price Per 100g: ${pricePer100g.toFixed(2)}
      </div>
    )}
  </section>

  <!-- 📘 Why This Matters + Pro Tips -->
  <section class="max-w-3xl mx-auto px-6 mt-6 mb-8">
    <h2 class="text-xl font-semibold mb-4 text-gray-800">Why This Matters</h2>
    <p class="text-gray-700 mb-4">
      Grocery items are often priced in confusing formats. Standardizing to 100g makes it easy to identify the best deal, regardless of package size.
    </p>

    <h3 class="text-lg font-semibold mb-2 text-gray-800">Pro Tips</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
      <li>Use for deli items, snacks, or anything sold by weight.</li>
      <li>Smaller packages often cost more per gram — but not always.</li>
      <li>This is especially helpful when shopping across brands or stores.</li>
    </ul>
  </section>
</Layout>


