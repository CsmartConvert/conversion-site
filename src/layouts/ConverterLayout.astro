---
const { title, description, icon, result, tablespoonsInput } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{title} | Smart Converter</title>
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body class="bg-gray-50 text-gray-800 font-sans">
    <main class="max-w-3xl mx-auto px-4 py-10">

      <header class="mb-6">
        <h1 class="text-2xl sm:text-3xl font-bold">{icon} {title}</h1>
        <p class="text-gray-600">{description}</p>
      </header>

      <!-- 💡 This is where the slot content from the tool page goes -->
      <slot />

      <!-- ✅ This result section is based on props -->
      {typeof result === 'number' && (
        <div aria-live="polite">
          <div class="p-4 bg-green-50 border border-green-300 text-green-800 rounded text-center">
            <strong>{tablespoonsInput}</strong> tablespoons = 
            <strong> {result.toFixed(2)}</strong> cups
          </div>

          <section class="space-y-4 mt-6">
            <h2 class="text-lg font-bold text-center">Tablespoons to Cups Chart</h2>
            <table class="min-w-full text-sm text-left border border-gray-300 bg-white shadow-sm rounded overflow-hidden">
              <thead>
                <tr class="bg-gray-100 text-gray-700">
                  <th class="py-2 px-4 border-b">Tablespoons</th>
                  <th class="py-2 px-4 border-b">Cups</th>
                </tr>
              </thead>
              <tbody>
                <tr><td class="py-2 px-4 border-b">1 tbsp</td><td class="py-2 px-4 border-b">0.06 cup</td></tr>
                <tr><td class="py-2 px-4 border-b">2 tbsp</td><td class="py-2 px-4 border-b">0.13 cup</td></tr>
                <tr><td class="py-2 px-4 border-b">4 tbsp</td><td class="py-2 px-4 border-b">0.25 cup</td></tr>
                <tr><td class="py-2 px-4 border-b">8 tbsp</td><td class="py-2 px-4 border-b">0.5 cup</td></tr>
                <tr><td class="py-2 px-4 border-b">12 tbsp</td><td class="py-2 px-4 border-b">0.75 cup</td></tr>
                <tr><td class="py-2 px-4 border-b">16 tbsp</td><td class="py-2 px-4 border-b">1 cup</td></tr>
              </tbody>
            </table>
            <p class="text-gray-600 text-xs text-center">
              16 tablespoons = 1 U.S. cup. Handy for doubling recipes and metric conversion!
            </p>
          </section>
        </div>
      )}

      <footer class="border-t mt-12 pt-6 text-center text-sm text-gray-500">
        &copy; {new Date().getFullYear()} Smart Converter. All rights reserved.
      </footer>
    </main>
  </body>
</html>



